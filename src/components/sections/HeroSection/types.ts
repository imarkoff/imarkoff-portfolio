export interface HeroAnimatorProps {
    greeting: {
        hiId: string;
        otherId: string;
    };
    nameId: string;
    taglineId: string;
    labelId: string;
    buttonWrapperClassName: string;
}